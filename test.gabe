fn add_one(i) {
    return i + 1;
}

let i = 10;

let b = 0;

while i {
    if i * 2 + (3 - i) {
        b = i * add_one(2);
    } else {
        return 1;
    }
    i = i - 1;
}

let arr = [1, 2, 3];

i = 0;
let total = 0;
while i < len(arr) {
    total = total + arr[i];
    i = i + 1;
}

fn call_twice(f, p) {
    return f(f(p));
}

len(arr) == call_twice(add_one, 1)

let object = {
    field_one: 1,
    field_two: 2 * 2,
    field_three: add_one
};

object.field_two
