fn add_one(i) {
    return i + 1;
}

let i = 10;

let b = 0;

while b < i {
    b = i * add_one(2);
    i = i - 1;
}

let arr = [1, 2, 3];

i = 0;
let total = 0;
while i < len(arr) {
    total = total + arr[i];
    arr[i] = arr[i] + 1;
    i = i + 1;
}

fn call_twice(f, p) {
    return f(f(p));
}


let object = {
    field_one: 1,
    field_two: 2 * 2,
    field_three: add_one,
    field_four: arr
};

object.field_four
